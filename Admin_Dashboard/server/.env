# Use the SAME database as the main server so pending signups appear here.
# Main server uses server/leftoverlink.db when run from server/ with the same URL.
# When running Admin server from Admin/server/, this path points to that file:
DATABASE_URL=sqlite+aiosqlite:///../../server/leftoverlink.db
